/**
 * Type Casting = by user
 * Type Coersion = by VM
 */

// Coersion
let x = "2" + 2    // > 22

// Casting 
let y = Number("2") + 2     // > 4

    // Boolean
        Boolean("abc")      // > true
        Boolean(0)      // > false